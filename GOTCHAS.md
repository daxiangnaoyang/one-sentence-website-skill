# 已知坑位

这些条目来自官方文档和本 Skill 边界测试；“已知”不表示当前账户已验证。执行前仍需读取当前平台文档和远端状态。

## 平台与采购

- Vercel Hobby 用于收费服务或商业主页：官方计划说明将 Hobby 限为个人、非商业用途。处理：先判断商业属性，核对当前计划和条款。
- Cloudflare Pages Direct Upload 后想打开 Git 自动部署：官方说明不能在同一项目直接切换。处理：一开始选 Git 集成，或新建项目迁移。
- Cloudflare Registrar 域名想改到其他 DNS，或 API 处理所有后缀/续费：Registrar 使用 Cloudflare nameservers，API 处于 beta 且功能有限。处理：按当前官方页面选择路径，注册前重新检查库存、价格和账户验证。
- 搜索结果显示域名可用但购买失败：搜索不是最终库存。处理：提交前再次 check；失败先查订单和域名状态，避免重复付款。
- VPS 停机仍产生费用：服务器资源仍存在。处理：核对提供商规则；不使用时按授权删除或降配，先保留备份/快照。

## 本地工具

- 私人备忘、`site.json` 或状态文件进入发布包：项目根目录不能整包上传。处理：`build` 白名单复制到 `dist`，发布前检查清单。
- 删除 `site.demo` 或只改标题后生产检查仍通过：演示状态和占位文案是两个门。处理：`site.demo` 必须是 boolean 且生产为 `false`；生产检查同时扫描源数据和产物。
- CSS 按钮样式消失：多 class 被错误清洗成一个 class。处理：逐个 class 清洗后用空格连接。
- `site.json` 出现 token/密码字段，或 HTML 里出现私钥：内容源和凭据混在一起。处理：凭据走平台 secrets/本地凭据存储，内容和产物检查拒绝敏感格式。
- 构建成功但图片、CSS 或锚点不存在：相对路径和静态资源没有验证。处理：运行 `check`，再用桌面和移动宽度回读页面。

## 证据边界

- CLI 返回成功就说“网站已上线”：CLI 不证明 DNS、TLS、缓存、内容验收和商业合规。处理：分别记录本地、平台、域名和公网页面证据。
- 回到任务后重新购买或建项目：只依赖对话记忆而没有读取状态和远端。处理：先读取状态、配置、部署 ID 和平台项目；冲突时标记过期并只补当前缺口。
